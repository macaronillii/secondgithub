version: '3.8'

services:
  mysql:
    image: mysql:8.4
    container_name: Mysql-database
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: